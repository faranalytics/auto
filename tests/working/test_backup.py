# import subprocess
# import time

# process = subprocess.Popen(['bash'], stdin=subprocess.PIPE, stdout=subprocess.PIPE, text=True, shell=True)

# process.stdin.write('echo "Hello from Python"\n')
# process.stdin.flush() # Ensure the data is sent immediately

# print(process.stdout.readline())
# print(process.stdout.readline())
# process.stdout.readline